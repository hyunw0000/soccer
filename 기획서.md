# REWIND — 감독의 90분

> 실제 월드컵 데이터 기반, **되감기(Rewind)** 를 핵심 메커니즘으로 삼은 축구 감독 인터랙션 웹서비스

- **서비스명 후보**: `REWIND` (부제: 감독의 90분) / `그때 그 선택` / `90+3`
- **한 줄 소개**: "그때 교체만 했어도." — 그 후회를 실제로 되돌려볼 수 있는 유일한 감독 시뮬레이터
- **문서 버전**: v1.2 (2026-07-30) — v1.0 0729 회의록 기반 / v1.1 선수 AI 시스템(§6) / v1.2 데이터 소스·라이선스 반영(§7.2)
- **관련 문서**: [`리소스_리서치.md`](./리소스_리서치.md) — 선수 AI 구현 리소스 및 데이터 라이선스 조사

---

## 1. 기획 의도

### 1.1 문제 정의

축구 감독 게임의 형식은 이미 정형화되어 있다. 포메이션 선택 → 선수 배치 → 경기 관전 → 교체. FIFA Online, FM 시리즈, 각종 전술보드 웹앱이 모두 이 루프를 공유한다. **여기에 같은 루프를 하나 더 만드는 것은 의미가 없다.**

우리가 주목한 것은 이 루프에 없는 감정이다.

> 실제 감독의 90분을 지배하는 감정은 "선택"이 아니라 **"후회"** 다.
> 모든 팬은 경기가 끝난 뒤에야 감독이 된다. "그때 그 선수를 뺐어야지."

기존 게임은 이 후회를 **다음 경기**로 미룬다. 되감기 버튼이 없기 때문이다. 우리는 그 버튼을 만든다.

### 1.2 차별점 — 세 가지 축

| 축 | 기존 축구 게임 | REWIND |
|---|---|---|
| **시간** | 단방향. 실수는 확정된다 | **되감기 가능.** 단, 자원이 소모된다 |
| **조작 대상** | 팀 단위 전술 프리셋 | **선수 1명 단위 명령.** 시간을 멈추고 개인에게 지시 |
| **결과물** | 스코어 1줄 | **분기 타임라인.** "내가 바꾼 역사" 자체가 공유 가능한 산출물 |

### 1.3 핵심 설계 원칙 (모든 의사결정의 기준)

1. **되감기는 공짜가 아니다.** 무한 되감기는 게임이 아니라 계산기다. 자원화·제약이 재미의 전부다.
2. **시뮬레이션은 결정론적이다.** 같은 상태 + 같은 명령 = 같은 결과. 그래야 "내 선택이 결과를 바꿨다"가 성립한다. 되감기 후 결과가 랜덤으로 달라지면 서비스의 존재 이유가 무너진다.
3. **감독은 조종하지 않는다. 지시한다.** 선수를 직접 움직이지 않는다. 지시는 지연되고, 선수의 능력치만큼만 이행된다.
4. **화면에 사람은 없다. 등번호만 있다.** 그래픽 경쟁을 하지 않는다. 전술판의 추상성이 곧 감독의 시점이다.

---

## 2. 타겟 & 핵심 경험

**타겟**: 축구를 "보는" 사람. 플레이 실력이 아니라 관점을 가진 사람.

**핵심 경험 시나리오 (3분)**

```
73분. 1-2로 지고 있다. 상대 왼쪽 윙어가 우리 오른쪽 풀백을 3번째 제쳤다.
  ↓ [스페이스바] 시간 정지
오른쪽 풀백(#2)을 클릭. 체력 41%. 지시: "전진성 ↓, 커버범위 ↓"
수비형 미드필더(#6)를 클릭. 지시: "오른쪽 커버 지원"
  ↓ [재생]
79분. 실점. 3번째 골. 왼쪽 측면이 아니라 중앙이 뚫렸다.
  ↓ [되감기] — 남은 직감 게이지 3 → 2
73분으로 복귀. 이번엔 시간이 완전히 멈춘 상태에서 드래그로 라인 자체를 내린다.
포메이션 4-4-2 → 5-3-2. 주장(#7)에게 "역습 전담" 지시.
  ↓ [재생]
88분. 역습. 동점골.
  ↓ 경기 종료
[분기 타임라인] 73분에서 갈라진 두 개의 90분이 나란히 재생된다.
```

이 마지막 화면 — **두 개의 평행 우주를 나란히 보여주는 리플레이** — 가 이 서비스의 시그니처이자 대중투표용 스크린샷이다.

---

## 3. 사용자 플로우

```
[0] 인트로
     │  감독 이름 입력 → "OOO 감독님, 부임을 환영합니다"
     ↓
[1] 스쿼드 구성        ← 2026 한국 대표팀 베이스 + 선수 풀 확장
     │  · K리그 / 해외파 / 국가대표 필터
     │  · 23인 스쿼드 확정
     │  · 주장(★) 1명 지정
     ↓
[2] 상대 스카우팅       ← 먼저 상대를 본다 (핵심)
     │  · 상대 포메이션 · 라인업 · 강점/약점 리포트
     │  · [AI 전술 브리핑] 버튼
     ↓
[3] 전술 구성
     │  · 포메이션 선택 → 포지션별 전술 기본값 자동 주입
     │  · 드래그로 11인 배치
     │  · 선수별 전술 6축 미세조정
     ↓
[4] 경기 (90분, 실시간 시뮬레이션)  ←──┐
     │  · 상시: 지시 슬라이더 / 교체 3회  │
     │  · 정지: 선수 클릭 → 명령          │ 되감기
     │  · 되감기: 직감 게이지 3회 ────────┘
     ↓
[5] 경기 리포트 & 분기 타임라인
        · 내가 만든 분기들의 비교 재생
        · 공유 카드 생성
```

---

## 4. 화면별 상세 설계

### [0] 인트로 — 부임

- 검은 화면, 커서 하나. `감독 이름을 입력하세요.`
- 입력 즉시 전술판이 페이드인. **로그인 없음.** 진입 마찰을 0으로.
- 감독 이름은 이후 모든 UI에 등장한다 (`홍길동 감독의 90분`, 공유 카드 서명).

### [1] 스쿼드 구성

| 요소 | 설계 |
|---|---|
| 기본 스쿼드 | 2026 월드컵 한국 대표팀 명단 프리셋 |
| 선수 풀 | K리그 + 해외 리그 + 국가대표 (필터/검색) |
| 카드 정보 | 등번호 · 이름 · 포지션 · 6대 능력치 레이더 · 소속 |
| 주장 지정 | ★ 1명. **주장은 전용 효과를 가진다** (아래) |
| 제약 | 23인, 포지션별 최소 인원 검증 |

**주장(최애캐) 시스템** — 회의에서 나온 "최애캐" 아이디어를 게임 메커니즘으로 승격:
- 주장은 **지시 이행 지연이 절반**이다 (감독의 의중을 가장 빨리 이해한다)
- 주장이 경기장에 있으면 반경 내 아군의 체력 감소가 완화된다
- 주장 교체/퇴장 시 팀 전체 지시 이행 속도 저하 → **주장을 언제 뺄 것인가**라는 긴장 생성

### [2] 상대 스카우팅 — "먼저 상대를 본다"

회의 결정사항(*"시작할 때 상대의 전술을 보고 우리 전술을 짠다"*)을 독립 화면으로 승격. 이 순서가 감독의 사고 순서이기 때문이다.

- 상대 포메이션 전술판 (읽기 전용, 등번호 표시)
- 상대 팀 스탯 리포트: 점유율 성향 / 빌드업 경로 / 측면 편중도 / 압박 라인 높이
- **AI 전술 브리핑**: 상대 데이터를 입력으로 "이 팀은 오른쪽 측면에 62% 편중되어 있습니다. 왼쪽 풀백의 커버범위를 넓히거나 3백을 고려하세요." 형태의 3줄 브리핑 + 추천 포메이션 2안
- AI는 **추천만 하고 적용하지 않는다.** 결정은 감독이 한다. (몰입감 원칙)

### [3] 전술 구성

**포메이션 프리셋** — 선택 시 포지션별 전술 기본값이 자동 주입된다.

```
4-3-3  →  LB: 전진성 70 / 폭 80 / 압박 60 ...
          CB: 전진성 20 / 폭 35 / 압박 45 ...
3-5-2  →  LWB: 전진성 85 / 폭 90 / 압박 65 ...
```

회의 결정(*"선수 별 전술 기본값을 별도로 빼자"*)에 따라 프리셋은 코드가 아닌 **데이터 테이블**(`formations.json`)로 분리한다. 밸런싱과 포메이션 추가가 코드 수정 없이 가능해진다.

**선수별 전술 6축** (모든 선수가 동일 스키마, 0~100)

| 축 | 의미 | 영향 |
|---|---|---|
| 전진성 | 기본 위치 대비 전진 정도 | 공격 가담 ↔ 뒷공간 노출 |
| 폭 | 중앙 ↔ 측면 성향 | 측면 과부하 ↔ 중앙 밀집 |
| 압박강도 | 볼 소유 상대에 대한 접근 | 탈취 확률 ↑ / **체력 소모 ↑↑** |
| 패스길이 | 짧은 패스 ↔ 롱볼 | 점유율 ↔ 전환 속도 |
| 리스크 | 안전 ↔ 모험적 선택 | 기회 창출 ↔ 턴오버 |
| 커버범위 | 담당 구역 넓이 | 커버 ↔ 개인 위치 이탈 |

**압박강도-체력**의 트레이드오프가 경기 후반 되감기 수요를 만드는 핵심 밸런싱 레버다.

### [4] 경기 화면 — 서비스의 심장

```
┌──────────────────────────────────────────────────────────────┐
│  KOR 1 - 2 BRA          73:14        ⏪ ⏪ ⏪  (직감 3)      │  ← 헤더
├───────────────────────────────────────────┬──────────────────┤
│                                           │  #2 김OO         │
│         ⑦                                 │  RB · 체력 41%   │
│    ②   ⑥      ⑨                          │ ───────────────  │
│  ①   ④   ⑤   ⑧    ③                     │  전진성  ▓▓▓░░   │
│              ⑩                            │  폭      ▓▓▓▓░   │
│         [ 필드: 등번호 + 체력 링 ]          │  압박    ▓▓▓▓▓   │
│                                           │  패스길이 ▓▓░░░  │
│                                           │  리스크  ▓▓░░░   │
│                                           │  커버범위 ▓▓▓░░  │
│                                           │ ───────────────  │
│                                           │  [교체] [지시]    │
├───────────────────────────────────────────┴──────────────────┤
│  ▶ 재생 / ⏸ 정지     [타임라인 ══════●════════]   ⏪ 되감기   │
└──────────────────────────────────────────────────────────────┘
```

**선수 표현** (회의 결정: *"등 번호로만"*)
- 원형 노드 + 등번호. 사람 일러스트 없음.
- **체력은 원의 테두리 링**으로 표현 (100% = 완전한 원, 소진될수록 링이 줄고 색이 청록→황→적으로 이동)
- 지시 이행 중인 선수는 노드가 미세하게 펄스 → "명령이 전달되고 있다"는 피드백
- 주장은 ★ 배지

**조작 계층 — 핵심 비대칭**

회의 미결정 사항(*"항상 수정할 수 있는 기능은?"*)에 대한 확정 설계:

| | **상시 (경기 진행 중)** | **정지 중** | **되감기 후** |
|---|---|---|---|
| 전술 6축 슬라이더 | ✅ (이행 지연 5~15초) | ✅ (재생 시 지연 적용) | ✅ (즉시 반영) |
| 선수 교체 | ✅ (3회 제한) | ✅ | ✅ |
| 포메이션 프리셋 전환 | ❌ | ✅ (전체 지연 15초) | ✅ (즉시) |
| **드래그 위치 재배치** | ❌ | ❌ | ✅ **되감기 전용** |
| 개별 역할(롤) 변경 | ❌ | ❌ | ✅ **되감기 전용** |

> **설계 근거**: 드래그 재배치를 상시 허용하면 게임은 실시간 마이크로 컨트롤로 변질된다. 반대로 되감기 후에만 허용하면 되감기는 "실수 취소"가 아니라 **"전면 재설계 권한"** 이라는 명확한 가치를 얻는다. 회의에서 나온 *"리와인드를 했을 때 드래그 기능을 넣고"* 를 그대로 규칙화한 것.

**지시 이행 지연** — 감독이 조종자가 아닌 이유
```
지연 시간 = 기본 10초 × (1 - 선수 전술이해도/200) × (주장이면 0.5)
        → 이해도 90 선수: 5.5초 / 이해도 40 선수: 8초 / 주장: 절반
```
슬라이더를 움직여도 즉시 바뀌지 않는다. 진행 바가 채워지는 동안 감독은 기다린다. 이 지연이 **"교체를 언제 준비할 것인가"** 라는 진짜 감독 판단을 만든다.

### [5] 경기 리포트 & 분기 타임라인 — 시그니처 화면

경기 종료 후, 이 서비스의 결론.

```
        73:14  ← 분기점
  ────────┬──────────────────────  [분기 A]  1-3 패
          │     "풀백 전진성만 낮췄다"
          └──────────────────────  [분기 B]  2-2 무  ★채택
                "5-3-2 전환 + 주장 역습 전담"
```

- 분기점마다 **무엇을 바꿨는지 diff**가 자동 생성된다 (`전진성 70→30`, `포메이션 4-4-2→5-3-2`)
- 두 분기를 **나란히 동시 재생**. 같은 시각의 두 우주를 좌우로 본다.
- 최종 공유 카드: `홍길동 감독 | 3번의 되감기로 1-3을 2-2로 | KOR vs BRA`
- 이 카드가 대중투표 유입 자산이다.

---

## 5. 되감기 시스템 — 상세 규칙

서비스의 유일한 차별점이므로 규칙을 가장 엄밀하게 정의한다.

### 5.1 자원: 감독 직감 게이지

- 경기당 **3회**. 회복 없음.
- 후반 시작 시 1회 추가 지급(총 4회)하는 안은 밸런싱 테스트 후 결정.

### 5.2 되감기 단위

| 방식 | 설명 | 채택 |
|---|---|---|
| 고정 시간 (-60초) | 단순, 예측 가능 | 🔺 대안 |
| **직전 이벤트 시점** | 실점/퇴장/부상 등 마지막 주요 이벤트 직전으로 | ✅ **채택** |
| 자유 스크럽 | 타임라인 아무 지점 | ❌ (게임성 붕괴) |

**채택 근거**: 감독의 후회는 "1분 전"이 아니라 **"그 실점 직전"** 에 걸려 있다. 이벤트 기준 되감기가 감정선과 일치하며, 되감기 버튼에 `⏪ 68:22 실점 직전으로` 같은 명시적 라벨을 붙일 수 있어 학습 비용도 낮다.

### 5.3 제약

1. **되감기 쿨다운 5분(게임 시간).** 연속 되감기로 한 장면을 무한 탐색하는 것을 차단.
2. **되감기해도 체력은 복원되지만 교체 횟수는 복원되지 않는다.** → 교체를 되돌려 다시 쓰는 악용 차단. (교체 카드는 "감독 인생의 자원")
3. **후반 85분 이후 되감기 불가.** 마지막 5분은 확정. 결말의 무게를 지킨다.
4. 되감기 후 **아무것도 바꾸지 않고 재생하면 결과가 100% 동일**하다 (결정론). 되감기는 요행이 아니라 재설계 수단임을 시스템이 증명한다.

### 5.4 기술 구현

- 매 **5초마다 전체 게임 상태 스냅샷**을 링 버퍼에 저장 (90분 = 1,080 스냅샷)
- 상태 = `{ tick, seed, players[], ball, score, events[], stamina[] }` — 순수 직렬화 가능 객체
- 되감기 = 스냅샷 복원 + **PRNG 시드 복원**. 시드를 복원하지 않으면 결정론이 깨진다. (가장 중요한 구현 포인트)
- 분기는 트리 구조로 보존 → 리포트 화면에서 재생

---

## 6. 시뮬레이션 엔진 & 선수 AI

> **핵심**: 22명의 선수는 각각 독립적인 AI 에이전트다. 감독은 팀을 조종하는 것이 아니라, **11개 AI의 판단 기준을 조정**한다. 전술 6축 슬라이더는 위치 오프셋이 아니라 **효용 함수의 가중치**다.

### 6.1 원칙

- **순수 함수형 · 결정론적**: `step(state, seed) → nextState`. DOM/시간/전역 상태 의존 없음.
- **Web Worker에서 실행**: 메인 스레드는 렌더링만. 60fps 보장.
- **틱 = 100ms 게임 시간**, 재생 속도 ×1 / ×2 / ×4.
- 90분 = 54,000틱. 프리컴퓨트가 아닌 실시간 진행 (되감기 후 분기가 생기므로).

### 6.2 왜 인게임 LLM이 아닌가

선수 AI에 LLM을 쓰지 않는다. 세 가지 이유 모두 치명적이다:

| 이유 | 내용 |
|---|---|
| **결정론 붕괴** | LLM은 동일 입력에 동일 출력을 보장하지 않는다. §1.3 원칙 2가 깨지면 되감기가 무의미해진다 |
| **지연** | 22명 × 초당 2회 판단 = 초당 44 추론. 실시간 불가 |
| **비용** | 90분 경기 1회당 수만 회 호출 |

대신 **효용 기반 AI(Utility AI)** 를 쓴다. 결정론적이고, 1ms 미만이고, **점수를 그대로 UI에 노출할 수 있다**(§6.9). LLM은 경기 밖(스카우팅 브리핑·분기 해설, §9)에서만 쓴다.

### 6.3 선수 AI 3계층

```
 ┌─ 인지 (Perception) ──── 이 선수는 지금 무엇을 "보는가"     → 스탯: 시야·축구지능
 │      ↓  제한된 후보 목록
 ├─ 판단 (Decision) ────── 각 선택지의 효용 점수 계산·최댓값   → 감독의 6축 = 가중치
 │      ↓  선택된 행동
 └─ 실행 (Execution) ───── 그 행동을 얼마나 잘 해내는가        → 스탯: 패스·드리블·속도 + 체력
```

이 3분할이 게임의 드라마를 만든다. 같은 상황에서
- **인지 실패** → 좋은 패스 길이 있는데 못 본다 (백패스)
- **판단 실패** → 봤지만 감독 지시(리스크 ↓) 때문에 안전한 선택을 한다
- **실행 실패** → 옳은 선택을 했지만 발끝에서 어긋난다

감독이 개입할 수 있는 지점은 **판단**뿐이다. 나머지 둘은 선수 선발(스쿼드 구성)의 영역이다. → §1의 "스쿼드 구성"과 "전술 구성"이 각각 다른 층위를 담당하게 된다.

### 6.4 인지 — 선수는 전장을 다 보지 못한다

매 판단 시점에 선수는 **모든 선택지를 얻지 못한다.**

```
인지 후보 수 = round( 3 + (시야스탯/100) × 5 )        // 3 ~ 8개
인지 반경    = base × (1 + 축구지능/150) × 압박보정
압박보정     = 1 - (나를 압박하는 상대 수 × 0.15)      // 몰리면 시야가 좁아진다
```

- 후보는 **거리순이 아니라 "눈에 띄는 순"** 으로 선별한다: 가까움 + 프리 여부 + 진행 방향 정합
- 시야 60인 수비수는 반대편에 완전히 비어 있는 윙어를 **후보 목록에 넣지 못한다**. 그래서 백패스한다. 이것은 버그가 아니라 그 선수의 한계다.
- 압박강도 지시를 높이면 상대의 인지 반경이 줄어든다 → **압박의 효과가 확률 보정이 아니라 상대 AI의 정보량 감소로 구현된다.** 훨씬 축구답고, 화면에서 눈에 보인다.

### 6.5 판단 — 효용 함수와 6축의 직결

선수는 후보 행동마다 점수를 매기고 최댓값을 고른다. **가중치 `w`가 곧 감독이 만진 슬라이더다.**

**온볼(볼 소유 시) 행동 후보**: `패스→동료j` / `드리블(방향d)` / `슈팅` / `키핑` / `클리어`

```
U(패스→j) =  w_전진  × 전진이득(j)                  // 상대 골문까지 거리 단축량
           + w_안전  × 성공확률(j)                  // 아래 실행 모델과 동일 식
           + w_폭    × 측면정합(j)                  // 폭 지시와 j의 좌우 위치 일치도
           − w_길이  × |거리(j) − 선호패스거리|       // 패스길이 지시에서 벗어난 정도
           + 0.15 × (j가 주장 ? 1 : 0)              // 주장에게 볼이 몰리는 자연스러운 편향

U(슈팅)   =  w_전진 × 기대득점(각도, 거리) − w_안전 × (1 − 슈팅성공확률)
U(드리블) =  w_전진 × 전진이득(d) + w_리스크 × 돌파기대 − 압박밀도(d)
```

| 슬라이더 | 효용 가중치로의 변환 |
|---|---|
| **전진성** | `w_전진 = 전진성/100` — 높을수록 전진 옵션 점수 급등 |
| **리스크** | `w_안전 = 1 − 리스크/100` — 낮을수록 성공확률 항의 비중 지배 |
| **폭** | `w_폭` — 측면 옵션 가산 |
| **패스길이** | `선호패스거리 = 8m + (패스길이/100)×32m` |
| **압박강도** | 오프볼 효용의 "볼 접근" 항 가중 |
| **커버범위** | 오프볼 효용의 "담당구역 이탈" 페널티 계수 |

**오프볼(볼 미소유 시) 행동 후보**: 기본 위치 주변 **5×5 그리드(25개 후보 좌표)** 를 샘플링해 점수화

```
U(위치p) =  w_전진   × 전방성(p)
          + w_폭     × 측면성(p)
          + w_압박   × 볼접근보너스(p)              // 수비 시
          + 공간가치(p)                             // 공격 시: 상대 밀도 낮은 곳
          − w_커버   × 담당구역이탈(p)
          − 이동비용(p) × 체력페널티                 // ★
          
체력페널티 = 1 + max(0, (60 − 체력) / 25) ^ 2       // 체력 60 미만부터 급격히 증가
```

★ **체력이 AI 판단에 직접 개입한다.** 체력 40인 풀백은 오버래핑의 이동비용 페널티가 너무 커져서 **스스로 전진을 포기한다.** 감독이 전진성 80을 지시해도 뛰지 않는다.

> 이것이 이 설계의 가장 중요한 창발(emergent) 동작이다. "지친 선수가 복귀를 포기해서 실점" 이라는 축구의 대표적 장면이 **스크립트 없이** 나온다. 그리고 이것이 §5 되감기의 최대 동기가 된다 — *"교체를 미리 준비했어야 했다."*

### 6.6 전술이해도 = 지시의 왜곡

전술이해도는 §4의 "지시 이행 지연"뿐 아니라 **가중치 정확도**에도 작용한다.

```
실제_적용_가중치 = 지시_가중치 + noise(seed, playerId) × (1 − 이해도/100) × 0.4
```

- 이해도 90 → 감독 지시를 거의 그대로 반영
- 이해도 45 → 전진성 80을 지시해도 실제로는 55~95 사이에서 흔들린다
- noise는 **시드 기반이라 되감기 시 동일하게 재현**된다. "운이 나빴다"가 아니라 "그 선수는 원래 그렇다"

→ 스쿼드 구성 화면에서 능력치만 보고 뽑으면 전술이 안 먹힌다. **선발과 전술이 서로를 구속**하는 관계가 성립한다.

### 6.7 실행 — 결과 판정

판단이 끝난 행동을 능력치로 판정한다. 모든 난수는 `seededRandom(tick, playerId, actionId)`.

```
패스성공확률 = sigmoid( 패스스탯 × 체력승수
                      − 거리계수 × 패스거리
                      − 0.8 × 압박받는수
                      + 수신자.퍼스트터치 × 0.3 )

탈취확률    = (수비.수비력 × 체력승수) / (수비.수비력 + 공격.드리블)
              × (1 + 압박강도/200) × 거리감쇠

체력승수    = 0.55 + 0.45 × (체력/100)
```

**의도적 단순화**: 물리 엔진 없음. 볼은 이벤트 간 보간 이동. 우리는 축구 시뮬레이터가 아니라 **전술 결정의 결과를 보여주는 장치**를 만든다. 시각적 그럴듯함 > 물리적 정확성.

### 6.8 틱 파이프라인 & 성능 예산

```
1. 지시 이행 큐 처리      (지연 만료된 명령 → 가중치 반영, 이해도 noise 적용)
2. 체력 갱신             (압박강도·이동거리 비례 소모)
3. 선수 AI 판단          (스태거: 매 틱 22명 중 2~3명만 재평가)
   ├ 인지: 후보 선별
   ├ 판단: 효용 점수 계산 → 행동 확정
   └ 캐시: 다음 재평가까지 행동 유지
4. 이동/실행            (전 선수, 확정된 행동 수행)
5. 볼 이벤트 판정         (시드 기반)
6. 이벤트 발행           (골/파울/부상/경고)
7. 5틱마다 스냅샷 저장
```

**판단 주기**: 500ms(5틱)마다. 단, **볼 소유자와 볼 반경 15m 내 선수는 100ms마다** 재평가.
**스태거링**: `playerId % 5 === tick % 5` 로 분산 → 틱당 평균 4~5명 재평가.

| 항목 | 예산 |
|---|---|
| 틱당 AI 평가 | 약 5명 × 25후보 = 125회 효용 계산 |
| 틱당 목표 시간 | **< 2ms** (100ms 예산의 2%) |
| ×4 배속 시 | 25ms/실시간초 — 여유 충분 |

### 6.9 판단 가시화 — "왜 그랬어?" 패널 ★

효용 AI를 선택한 결정적 이유. **선수의 사고를 그대로 화면에 띄운다.**

경기 정지 → 선수 클릭 시 지시 패널 하단에:

```
 #2 김OO 의 현재 판단                    체력 41%
 ────────────────────────────────────────────────
 ▸ 제자리 유지          68 ████████░░
 ▸ 중앙 커버 복귀       35 ████░░░░░░
 ▸ 전진 오버래핑        12 █░░░░░░░░░
      └ 감독 지시 +41 / 체력 페널티 −52  ⚠
 ────────────────────────────────────────────────
 "지시는 전달됐지만, 뛸 힘이 없습니다."
```

이 패널의 가치:
- **몰입감(25점)**: 감독이 "선수의 머릿속"을 읽는 경험. 다른 어떤 축구 게임에도 없다.
- **되감기 동기 직결**: 왜 실패했는지 *숫자로* 보이니, 무엇을 되돌려야 할지 알게 된다. 되감기가 운 시험이 아니라 **추론**이 된다.
- **개발 비용 0**: AI가 이미 계산한 점수를 그대로 렌더링하는 것뿐이다. 디버그 뷰가 곧 핵심 기능이 되는 구조.

### 6.10 결정론 보장 체크리스트

선수 AI 도입으로 결정론 리스크가 커졌다. M1에서 다음을 테스트로 고정한다:

- [ ] 모든 난수는 `seededRandom(tick, playerId, actionId)` 경유 — `Math.random` 전역 금지 (ESLint 규칙)
- [ ] 효용 동점 시 **playerId 오름차순**으로 타이브레이크 (정렬 불안정성 차단)
- [ ] 인지 후보 선별 결과를 `Map` 순회 대신 명시적 정렬 배열로 확정
- [ ] 부동소수점은 고정소수점(정수 ×1000) 연산으로 통일
- [ ] 스태거 순서는 `tick` 기반 순수 함수 — 실행 순서에 의존 금지
- [ ] **회귀 테스트**: 동일 시드 90분 2회 실행 → 최종 상태 해시 완전 일치

---

## 7. 데이터 설계

### 7.1 선수 스키마

```ts
type Player = {
  id: string;
  name: string;          // 한글/영문
  number: number;        // 표시의 전부
  positions: Position[]; // 주포지션 + 부포지션
  league: 'K1' | 'OVERSEAS' | 'NT';
  club: string;
  // 실행 능력치 — 카드에 노출 (0-100 정규화)
  attrs: {
    pace: number; shooting: number; passing: number;
    dribbling: number; defending: number; physical: number;
  };
  // 인지 능력치 — AI 3계층 중 '인지' 담당 (§6.4)
  cognition: {
    vision: number;      // 시야 → 인지 후보 개수
    iq: number;          // 축구지능 → 인지 반경, 공간가치 평가 정확도
    firstTouch: number;  // 퍼스트터치 → 패스 수신 성공률
  };
  stamina: number;        // 최대 체력
  understanding: number;  // 전술이해도 → 지시 이행 지연 + 가중치 왜곡 (§6.6)
};
```

**인지 능력치 분리의 기획적 의미**: 기존 축구 게임의 능력치는 전부 "실행"이다. 우리는 **시야·축구지능**을 별도 축으로 세워 스쿼드 구성 단계에 새로운 판단을 만든다 — *"패스는 좋은데 시야가 낮은 선수"* 는 좋은 선택지를 못 본다. 능력치가 곧 AI의 입력 파라미터이므로, 선발이 전술만큼 결과를 바꾼다.

### 7.2 데이터 소스 및 처리

> 상세 조사·라이선스 검토는 **`리소스_리서치.md` §5** 참조.

| 데이터 | 출처 | 처리 |
|---|---|---|
| **기본 시나리오 경기** | **2022 카타르 월드컵 16강 KOR vs BRA** (StatsBomb Open Data) | 라인업·포메이션·스코어 |
| 팀 전술 성향 | 동 대회 이벤트 데이터 집계 | 측면 편중도·라인 높이·압박 위치 → % 지표 |
| 선수 실행 능력치 | 동 대회 + K리그 데이터 포털 | 패스 성공률·드리블·xG 등 → 0-100 정규화 |
| **선수 인지 능력치** (시야·축구지능) | **이벤트 데이터에서만 산출 가능** | 전진패스 거리 분포·라인 브레이킹 빈도 |
| K리그 선수 풀 | K리그 통합 데이터 포털 | 동일 스키마 매핑 |

**⚠ StatsBomb 라이선스 제약이 파이프라인 구조를 강제한다**

약관상 **데이터의 재배포·복제가 금지**되어 있고(1.2.1·7조), **파생 분석의 공개만 허용**된다(서문). 따라서:

```
[로컬 · 빌드 타임만]                      [배포물]
StatsBomb 원본 JSON  ──집계/정규화──▶   파생 스칼라만
  · .gitignore 등록                       · { passing: 78, vision: 64 }
  · 번들 제외                             · 팀 성향 % 지표
  · 절대 커밋 금지                        · 원본 복원 불가능
                                        + StatsBomb 로고 표기 (약관 1.4 의무)
```

- **원칙: 데이터를 나르면 위반, 분석을 나르면 허용.**
- 변환 스크립트 `scripts/build-data.ts` 로 분리 — 원본 → 게임 스키마 변환을 재현 가능하게 유지
- **전부 빌드 타임에 정적 JSON으로 확정.** 런타임 외부 API 의존 없음 → 배포 안정성(평가항목 "완성도" 25점 직결)
- 라이선스 리스크가 부담될 경우의 **폴백안**(공개 사실 + K리그 포털 + 수기 산정)은 `리소스_리서치.md` §5.1.2에 준비되어 있다

---

## 8. 기술 스택 & 아키텍처

| 레이어 | 선택 | 근거 |
|---|---|---|
| 프레임워크 | **Next.js (App Router) + TypeScript** | Vercel 배포 최적, 정적 데이터 번들 용이 |
| 상태 | **Zustand** | 시뮬 상태와 UI 상태 분리, 스냅샷/복원에 유리 |
| 시뮬 엔진 | **순수 TS 모듈 + Web Worker** | 프레임워크 무관, 테스트 가능, 렌더 블로킹 없음 |
| 필드 렌더 | **SVG + Framer Motion** (22노드 규모에서 Canvas 불필요) | 노드별 접근성/이벤트 핸들링 간편, 애니메이션 선언적 |
| 스타일 | **Tailwind + shadcn/ui** | 속도, 다크 테마 일관성 |
| AI | **Vercel AI Gateway** (전술 브리핑) | 프로바이더 종속 없이 스트리밍 브리핑 |
| 배포 | **Vercel** | 프리뷰 URL로 팀 내 검증 루프 단축 |

```
app/
  (flow)/intro · squad · scout · tactics · match · report
lib/
  sim/          engine.ts  events.ts  rng.ts                ← 순수, 테스트 대상
  sim/ai/       perceive.ts  decide.ts  utility.ts  act.ts  ← 선수 AI 3계층 (§6.3)
  sim/ai/weights.ts                                          ← 6축 → 효용 가중치 변환
  sim/snapshot.ts  branch.ts                                ← 되감기 코어
  data/         players.json  formations.json  teams.json
workers/
  sim.worker.ts
components/
  Pitch/  PlayerNode  TacticSlider  Timeline  BranchDiff
```

**저장**: 서버 DB 없음. 진행 상태는 `localStorage`, 공유 카드는 결과 상태를 URL 파라미터로 인코딩(무DB 공유). → 인프라 리스크 0.

---

## 9. AI 기능 (경계 명확화)

AI는 **감독을 대체하지 않고 코치진 역할만** 한다. 몰입감(25점) 항목을 해치지 않기 위한 원칙이다.

| 기능 | 시점 | 출력 |
|---|---|---|
| **상대 스카우팅 브리핑** | 경기 전 | 상대 데이터 → 3줄 요약 + 포메이션 2안 (적용은 안 함) |
| **하프타임 코치 코멘트** | 45분 | 전반 스탯 → "오른쪽이 계속 뚫립니다" 1~2줄 |
| **분기 해설** | 리포트 | 두 분기 diff → "당신의 5-3-2 전환이 실점을 막았습니다" |

세 기능 모두 **실패해도 게임이 진행된다** (graceful degradation). AI 호출 실패 시 규칙 기반 폴백 문구 출력. 데모 중 API 장애로 서비스가 멈추는 사고를 원천 차단.

---

## 10. 평가 기준 대응

| 평가 항목 | 배점 | 대응 설계 |
|---|---|---|
| **참신성** | 30 | 되감기 = 축구 감독 게임에 없던 메커니즘. "실수 취소"가 아니라 **자원 제약이 걸린 분기 탐색**으로 게임화. 분기 타임라인이라는 새 결과물 형식 + **선수 22명 독립 AI의 판단 과정 공개** |
| **감독 경험 설계** | 25 | 상대 스카우팅 선행 / 지시 이행 지연 / 조종 아닌 명령 / 주장 시스템 / 등번호만의 추상 전술판. 결정적으로 **판단 가시화 패널(§6.9)** — 선수의 사고를 숫자로 읽는 경험은 기존 축구 게임에 존재하지 않는다 |
| **완성도** | 25 | 결정론적 순수 엔진 + Web Worker + 정적 데이터 + 무DB + AI 폴백. **외부 의존 지점을 전부 제거**해 데모 중 실패 확률 최소화 |
| **기획/구현 일관성** | 20 | "되감기"라는 단일 명제에서 모든 기능이 파생. 제약 규칙 4개, 조작 계층 비대칭 표가 기획 의도를 그대로 코드 구조로 반영 |

**1차 대중투표 대응**: 분기 타임라인 공유 카드 + 되감기 순간의 GIF성 인터랙션 → 스크린샷 한 장으로 차별점이 전달되는 화면 확보.

---

## 11. 개발 로드맵

| 단계 | 산출물 | 검증 기준 |
|---|---|---|
| **M1. 엔진 코어** | 결정론적 틱 엔진, 스냅샷/복원, 시드 RNG | 같은 시드 2회 실행 결과 완전 일치 (§6.10 체크리스트 전항목) |
| **M1.5 선수 AI** | 인지/판단/실행 3계층, 효용 함수, 6축→가중치 변환 | 슬라이더 변경이 실제 행동 분포를 바꾸는지 계측. 틱당 AI < 2ms |
| **M2. 전술판** | 필드 SVG, 등번호 노드, 체력 링, 드래그 배치, **판단 가시화 패널** | 22명 렌더 60fps 유지 |
| **M3. 되감기** | 직감 게이지, 이벤트 기준 복원, 분기 트리 | 되감기 후 무변경 재생 시 결과 100% 동일 |
| **M4. 플로우** | 인트로 → 스쿼드 → 스카우팅 → 전술 → 경기 | 처음 방문자가 설명 없이 경기 시작까지 도달 |
| **M5. 리포트** | 분기 비교 재생, 공유 카드 | 두 분기 동시 재생 정상 |
| **M6. AI + 폴리싱** | 브리핑/하프타임/해설, 밸런싱 | AI 전면 차단 상태에서도 완주 가능 |

**우선순위 원칙**: M1·M3(되감기 코어)이 실패하면 서비스에 남는 것이 없다. **엔진 결정론 확보 전에는 UI에 시간을 쓰지 않는다.**

---

## 12. 리스크 & 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 결정론 깨짐 (시드 복원 누락, 부동소수점 편차) | **치명적** — 서비스 명제 붕괴 | M1에서 회귀 테스트 고정. 부동소수점은 고정소수점(정수 ×1000) 연산으로 통일 |
| 시뮬 결과가 지루/비현실적 | 몰입 저하 | 물리 정확성 포기, 이벤트 밀도 중심 튜닝. "볼 만한가"를 기준으로 밸런싱 |
| **효용 가중치 밸런싱 실패** — 슬라이더를 바꿔도 행동이 안 바뀌거나, 한 축이 나머지를 압도 | **치명적** — 전술 조작이 무의미해짐 | M1.5에서 **행동 분포 계측 도구** 선행 구축: 축을 0↔100으로 흔들며 100경기 배치 실행, 패스방향·전진거리 분포 변화를 수치로 확인. 가중치는 `weights.ts` 상수 테이블로 분리해 코드 수정 없이 튜닝 |
| **AI 판단 진동(oscillation)** — 두 행동 효용이 비슷해 매 판단마다 뒤집힘 | 선수가 제자리에서 떨림 | 현재 행동에 **관성 보너스 +10%** 부여. 판단 주기 500ms 유지로 물리적 억제 |
| 되감기 남용으로 게임성 소멸 | 참신성 상쇄 | 3회 + 쿨다운 + 교체 미복원 + 85분 차단 (§5.3) |
| 조작 복잡도 과다 | 직관성 감점 | 기본 조작은 **정지 → 선수 클릭 → 슬라이더** 3동작으로 고정. 나머지는 전부 선택적 |
| 데이터 확보 지연 | 일정 리스크 | M1~M3는 더미 데이터로 진행. 데이터는 스키마만 먼저 고정 |

---

## 13. 회의 미결정 사항 처리 현황

| 0729 회의 안건 | 상태 | 결정 내용 |
|---|---|---|
| 차별점 = 되감기? | ✅ 확정 | 되감기를 유일 축으로. 자원화 + 4개 제약 (§5) |
| 감독 이름 커스텀 | ✅ 반영 | 인트로 화면, 전 UI 노출 |
| 주장(최애캐) 선택 | ✅ 승격 | 단순 선택 → 지시 지연 절반 + 체력 보조 메커니즘 (§4-[1]) |
| 선수 외관 = 등번호만 | ✅ 반영 | 원형 노드 + 체력 링 |
| 선수별 전술 기본값 분리 | ✅ 반영 | `formations.json` 데이터 테이블 |
| 게임 화면 구성 | ✅ 확정 | 정지 → 선수 클릭 → 우측 지시 패널 (§4-[4] 와이어프레임) |
| 전술 변경 시점 | ✅ 확정 | 실시간 + 되감기 제약. 조작 계층 3단 비대칭 표 |
| "항상 수정할 수 있는 기능"의 범위 | ✅ 확정 | 슬라이더·교체만 상시 / 드래그 재배치는 되감기 전용 |
| 후반 직감 게이지 추가 지급 | 🔺 보류 | 밸런싱 테스트 후 결정 (M6) |
| 되감기 단위: 고정시간 vs 이벤트 | ✅ 확정 | 이벤트 기준 (§5.2) |
| **선수별 AI 판단 시스템** (v1.1 추가) | ✅ 확정 | 효용 기반 AI 3계층. 전술 6축 = 효용 가중치, 스탯 = 인지/실행 (§6) |
| 효용 가중치 초기값 | 🔺 보류 | 행동 분포 계측 도구로 M1.5에서 튜닝 |
