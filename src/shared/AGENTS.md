# Shared 영역 작업 규칙

## 소유 역할

담당자 1 — 공용 파일 최종 통합

## 수정 허용

- 담당자 1만 `src/shared/**`를 최종 수정한다.

## 다른 담당자

- 읽기와 사용은 가능하다.
- 필요한 변경은 요청서나 PR 제안으로 제출하고 직접 수정하지 않는다.

## 경계 규칙

- 특정 기능에만 필요한 코드를 shared로 올리지 않는다.
- shared는 feature/app 내부 파일을 import하지 않는다.
- 공용 공개 API는 `src/shared/index.js`로 제공한다.
